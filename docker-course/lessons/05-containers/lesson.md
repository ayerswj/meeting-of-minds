# Lesson: Managing Containers

## Running Containers
- `docker run <image>` — Start a new container
- `-d` (detached), `-p` (port mapping), `--name` (name container)

## Stopping and Removing Containers
- `docker stop <container>` — Stop a running container
- `docker rm <container>` — Remove a stopped container

## Inspecting Containers
- `docker inspect <container>` — View detailed info
- `docker logs <container>` — View logs
- `docker stats` — Resource usage

## Container Lifecycle
- Created → Running → Stopped → Removed

---

Continue to the exercises to practice container management.
-- Exercises: Setting Up PostgreSQL

-- 1. Create a new database called 'practice_db'
CREATE DATABASE practice_db;

-- 2. Connect to the 'practice_db' database (psql command)
-- psql -U postgres -d practice_db

-- 3. List all databases in your PostgreSQL instance
-- In psql:
\l

-- 4. Drop the 'practice_db' database
DROP DATABASE practice_db;